# action.yml
name: 'Check Content'
description: 'Check content contains all necessary components'
# inputs:
#   who-to-greet:  # id of input
#     description: 'Who to greet'
#     required: false
#     default: 'World'
outputs:
  time: # id of output
    description: 'The time we greeted you'
runs:
  using: 'docker'
  image: 'Dockerfile'
  # args:
  #   - ${{ inputs.who-to-greet }}